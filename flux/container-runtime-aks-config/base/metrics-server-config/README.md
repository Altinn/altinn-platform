# Metrics Server Configuration

This directory contains the ConfigMap for Kubernetes metrics-server that configures the nanny (autoscaling) settings.

The configuration defines base CPU/memory resources and per-node scaling factors for the metrics-server deployment.