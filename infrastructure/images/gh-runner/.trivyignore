# Packages provided by Github through their runner image. Should be handled upstream.

# cross-spawn: ReDoS via improper input sanitization in crafted strings
CVE-2024-21538

# crypto/x509: Policy validation bypass when ExtKeyUsageAny is used in VerifyOptions.KeyUsages
CVE-2025-22874

# brace-expansion: DoS via unbounded brace range expansion
CVE-2026-25547

# minimatch: DoS via specially crafted glob patterns
CVE-2026-26996

# node-tar: Arbitrary file overwrite via symlink poisoning through Unicode path collision race condition
CVE-2026-23950

# node-tar: Arbitrary hardlink creation outside extraction root due to mismatched path resolution semantics
CVE-2026-24842

# node-tar: Arbitrary file read/write via hardlink pointing outside extraction root
CVE-2026-26960

# minimatch: DoS via unbounded recursive backtracking via crafted glob string
CVE-2026-27903

# minimatch: DoS via catastrophic backtracking in glob expressions
CVE-2026-27904

# picomatch: ReDoS via crafted extglob patterns
CVE-2026-33671

# node-tar: Hardlink path traversal via drive-relative linkpath
CVE-2026-29786

# node-tar: File overwrite via drive-relative symlink traversal
CVE-2026-31802

# node-tar: Arbitrary file overwrite and symlink poisoning via unsanitized hardlink/symlink linkpath entries
CVE-2026-23745

# Go binaries (containerd, runc, docker, buildx) in base image. Should be handled upstream.

# crypto/tls: Unexpected session resumption
CVE-2025-68121

# net/url: Memory exhaustion in query parameter parsing
CVE-2025-61726

# archive/zip: Excessive CPU consumption when building archive index
CVE-2025-61728

# crypto/tls: TLS 1.3 handshake record handling
CVE-2025-61730

# database/sql: Postgres Scan Race Condition
CVE-2025-47907

# runc: /proc write misdirection via racing container with shared mounts enabling symlink following
CVE-2025-52881

# glob CLI: Command injection via -c/--cmd executes matches with shell:true
CVE-2025-64756

# crypto/x509: DoS via unbounded hostname error string construction causing quadratic runtime
CVE-2025-61729

# GnuPG: Out-of-bounds write in armor_filter via double index increment for crafted input
CVE-2025-68973

# go-jose/go-jose/v4: DoS via crafted JSON Web Encryption
CVE-2026-34986

# moby/spdystream: DoS via SPDY streaming
CVE-2026-35469

# opentelemetry-go SDK: Arbitrary code execution via PATH hijacking
CVE-2026-24051

# opentelemetry-go: BSD kenv command not using absolute path enables PATH hijacking
CVE-2026-39883

# google.golang.org/grpc: Authorization bypass due to improper HTTP/2 path validation
CVE-2026-33186

# stdlib crypto/tls: DoS vulnerability in certificate chain building
CVE-2026-32280

# stdlib crypto/x509: DoS via inefficient certificate chain validation
CVE-2026-32281

# stdlib crypto/tls: DoS when TLS peer sends multiple consecutive key update messages
CVE-2026-32283

# stdlib crypto/x509: Certificate validation bypass due to incorrect DNS constraint handling
CVE-2026-33810

# docker/docker (Moby): Authorization bypass vulnerability
CVE-2026-34040
