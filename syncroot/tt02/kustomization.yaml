apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
patches:
  - target:
      kind: Vault
      name: dis-sample-vault
    patch: |-
      - op: add
        path: /spec/tags/env
        value: tt02
