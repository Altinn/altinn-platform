---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: redises.redis.dis.altinn.cloud
spec:
  group: redis.dis.altinn.cloud
  names:
    kind: Redis
    listKind: RedisList
    plural: redises
    singular: redis
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.azureName
      name: AzureName
      type: string
    - jsonPath: .status.hostName
      name: HostName
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Redis is the Schema for the redises API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of Redis.
            properties:
              clientProtocol:
                default: Encrypted
                description: ClientProtocol selects between Encrypted (TLS) and Plaintext.
                  Defaults to Encrypted.
                enum:
                - Encrypted
                - Plaintext
                type: string
              evictionPolicy:
                default: NoEviction
                description: EvictionPolicy selects the database eviction policy.
                  Defaults to NoEviction.
                enum:
                - AllKeysLFU
                - AllKeysLRU
                - AllKeysRandom
                - VolatileLFU
                - VolatileLRU
                - VolatileRandom
                - VolatileTTL
                - NoEviction
                type: string
              highAvailability:
                default: true
                description: HighAvailability spreads the cluster across availability
                  zones. Defaults to true.
                type: boolean
              identityRef:
                description: IdentityRef points to the owning ApplicationIdentity
                  in the same namespace.
                properties:
                  name:
                    description: Name is the ApplicationIdentity name in the same
                      namespace.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              modules:
                description: Modules is the optional list of Redis modules enabled
                  on the database.
                items:
                  description: RedisModule enables a single optional Redis module
                    on the database.
                  properties:
                    args:
                      description: Args are optional, module-specific arguments.
                      type: string
                    name:
                      description: Name is the module identifier.
                      enum:
                      - RedisJSON
                      - RediSearch
                      - RedisTimeSeries
                      - RedisBloom
                      type: string
                  required:
                  - name
                  type: object
                type: array
              persistence:
                description: Persistence configures optional AOF / RDB persistence.
                  Defaults to no persistence.
                properties:
                  aof:
                    description: AOF enables append-only-file persistence with the
                      specified frequency.
                    enum:
                    - Always
                    - Every1Second
                    type: string
                  rdb:
                    description: RDB enables snapshot persistence with the specified
                      frequency.
                    enum:
                    - 1h
                    - 6h
                    - 12h
                    type: string
                type: object
                x-kubernetes-validations:
                - message: Only one of 'aof' or 'rdb' may be set
                  rule: '!(has(self.aof) && has(self.rdb))'
              serviceAccountRef:
                description: ServiceAccountRef points to the owning ServiceAccount
                  in the same namespace.
                properties:
                  name:
                    description: Name is the ServiceAccount name in the same namespace.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              sku:
                default: Balanced_B0
                description: SKU drives cluster capacity. Defaults to the smallest
                  Balanced tier.
                enum:
                - Balanced_B0
                - Balanced_B1
                - Balanced_B3
                - Balanced_B5
                - Balanced_B10
                - MemoryOptimized_M10
                - MemoryOptimized_M20
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags are optional user-provided tags propagated to Azure
                  resources.
                type: object
              version:
                description: Version is the Redis version (e.g. "7", "7.4"). Optional;
                  defaults to the ASO default.
                pattern: ^[0-9]+(\.[0-9]+)?$
                type: string
            type: object
            x-kubernetes-validations:
            - message: exactly one of identityRef or serviceAccountRef must be set
              rule: has(self.identityRef) != has(self.serviceAccountRef)
          status:
            description: Status defines the observed state of Redis.
            properties:
              accessPolicyAssignmentName:
                description: AccessPolicyAssignmentName is the name of the managed
                  access policy assignment.
                type: string
              azureName:
                description: AzureName is the computed Azure Redis Enterprise cluster
                  name.
                type: string
              clusterResourceId:
                description: ClusterResourceID is the ARM resource ID of the Redis
                  Enterprise cluster.
                type: string
              conditions:
                description: Conditions represent the current state of this Redis.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              databaseResourceId:
                description: DatabaseResourceID is the ARM resource ID of the Redis
                  Enterprise database.
                type: string
              hostName:
                description: HostName is the resolved DNS hostname of the cluster
                  (e.g. "<azureName>.<region>.redis.azure.net").
                type: string
              observedGeneration:
                description: ObservedGeneration is the latest generation reconciled
                  by the controller.
                format: int64
                type: integer
              ownerPrincipalId:
                description: OwnerPrincipalID is the resolved owner principal ID.
                type: string
              port:
                description: Port is the database client port (defaults to 10000 for
                  Redis Enterprise).
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
