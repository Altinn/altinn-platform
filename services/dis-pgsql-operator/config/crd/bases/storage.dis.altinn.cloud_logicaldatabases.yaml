---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: logicaldatabases.storage.dis.altinn.cloud
spec:
  group: storage.dis.altinn.cloud
  names:
    kind: LogicalDatabase
    listKind: LogicalDatabaseList
    plural: logicaldatabases
    singular: logicaldatabase
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LogicalDatabase is the Schema for the logicaldatabases API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of LogicalDatabase.
            properties:
              access:
                description: access defines the identity that should get access to
                  this logical database.
                properties:
                  app:
                    description: app is the runtime application principal.
                    properties:
                      name:
                        description: name is the Entra principal name.
                        minLength: 1
                        type: string
                      principalId:
                        description: principalId is the Entra principal object ID.
                        minLength: 1
                        type: string
                    required:
                    - name
                    - principalId
                    type: object
                  owner:
                    description: owner is the Entra group for the team that owns the
                      logical database.
                    properties:
                      name:
                        description: name is the Entra principal name.
                        minLength: 1
                        type: string
                      principalId:
                        description: principalId is the Entra principal object ID.
                        minLength: 1
                        type: string
                    required:
                    - name
                    - principalId
                    type: object
                required:
                - app
                - owner
                type: object
              deletionPolicy:
                default: Retain
                description: |-
                  deletionPolicy controls database cleanup when this resource is deleted.
                  Only Retain is supported in this API slice.
                enum:
                - Retain
                type: string
              name:
                description: |-
                  name is the PostgreSQL database name to create inside the shared server.
                  It must be unique per shared server.
                maxLength: 63
                minLength: 1
                type: string
              server:
                description: server identifies the same-namespace shared Database
                  server.
                properties:
                  name:
                    description: name is the same-namespace Database resource to use
                      as the shared server.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - access
            - name
            - server
            type: object
          status:
            description: status defines the observed state of LogicalDatabase.
            properties:
              conditions:
                description: conditions represent the current validation/provisioning
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              databaseName:
                description: databaseName is the PostgreSQL database name managed
                  by the operator.
                type: string
              host:
                description: |-
                  host is the PostgreSQL server host for this logical database.
                  It is populated in a later reconciliation slice.
                type: string
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              port:
                description: |-
                  port is the PostgreSQL server port for this logical database.
                  It is populated in a later reconciliation slice.
                format: int32
                type: integer
              validationErrors:
                description: validationErrors contains field-level validation failures.
                items:
                  description: |-
                    LogicalDatabaseValidationError captures a validation failure observed by the
                    controller.
                  properties:
                    field:
                      description: field is the JSON path of the invalid field.
                      minLength: 1
                      type: string
                    message:
                      description: message is a human-readable description of the
                        validation failure.
                      minLength: 1
                      type: string
                    reason:
                      description: reason is a machine-readable reason for the validation
                        failure.
                      minLength: 1
                      type: string
                  required:
                  - field
                  - message
                  - reason
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - field
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
