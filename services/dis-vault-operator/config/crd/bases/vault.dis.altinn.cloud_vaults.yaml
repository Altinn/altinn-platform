---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: vaults.vault.dis.altinn.cloud
spec:
  group: vault.dis.altinn.cloud
  names:
    kind: Vault
    listKind: VaultList
    plural: vaults
    singular: vault
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Vault is the Schema for the vaults API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of Vault.
            properties:
              externalSecrets:
                default: false
                description: ExternalSecrets enables operator-managed namespaced SecretStore
                  integration.
                type: boolean
              groupObjectId:
                description: |-
                  GroupObjectID grants an optional Entra group access to the Key Vault.
                  Lowercase is required to keep the identifier canonical in GitOps.
                pattern: ^[0-9a-f]{8}(-[0-9a-f]{4}){3}-[0-9a-f]{12}$
                type: string
              identityRef:
                description: IdentityRef points to the owning ApplicationIdentity
                  in the same namespace.
                properties:
                  name:
                    description: Name is the ApplicationIdentity name in the same
                      namespace.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              publicNetworkAccess:
                default: Enabled
                description: PublicNetworkAccess is constrained to Enabled in v1.
                enum:
                - Enabled
                type: string
              purgeProtectionEnabled:
                default: true
                description: PurgeProtectionEnabled controls purge protection. Defaults
                  to true.
                type: boolean
              sku:
                default: standard
                description: SKU is the Key Vault SKU. Defaults to standard.
                enum:
                - standard
                - premium
                type: string
              softDeleteRetentionDays:
                default: 90
                description: SoftDeleteRetentionDays controls soft-delete retention
                  period.
                maximum: 90
                minimum: 7
                type: integer
              tags:
                additionalProperties:
                  type: string
                description: Tags are optional user-provided tags propagated to Azure
                  resources.
                type: object
            required:
            - identityRef
            type: object
          status:
            description: Status defines the observed state of Vault.
            properties:
              azureName:
                description: AzureName is the computed Azure Key Vault name.
                type: string
              conditions:
                description: Conditions represent the current state of this Vault.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              externalSecretStoreName:
                description: ExternalSecretStoreName is the name of the managed SecretStore
                  when enabled.
                type: string
              observedGeneration:
                description: ObservedGeneration is the latest generation reconciled
                  by the controller.
                format: int64
                type: integer
              ownerPrincipalId:
                description: OwnerPrincipalID is the resolved owner principal ID.
                type: string
              ownerRoleAssignmentId:
                description: OwnerRoleAssignmentID is the ARM ID of the owner role
                  assignment.
                type: string
              resourceId:
                description: ResourceID is the ARM resource ID of the vault.
                type: string
              vaultUri:
                description: VaultURI is the HTTPS URI of the vault.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
