version: "2"
run:
  allow-parallel-runners: true
linters:
  default: none
  enable:
    - copyloopvar
    - dupl
    - errcheck
    - goconst
    - gocyclo
    - govet
    - ineffassign
    - lll
    - modernize
    - misspell
    - nakedret
    - prealloc
    - revive
    - staticcheck
    - unconvert
    - unparam
    - unused
  settings:
    goconst:
      ignore-tests: true
    revive:
      rules:
        - name: comment-spacings
        - name: import-shadowing
    modernize:
      disable:
        - omitzero
        - newexpr
  exclusions:
    generated: lax
    rules:
      - linters:
          - dupl
          - goconst
          - lll
        path: _test\.go
      - linters:
          - dupl
          - lll
        path: internal/.*
      # manifests/ spells out each Kubernetes object as a literal, so label
      # keys such as "app" repeat across the labels and the selectors that
      # match them. Hoisting them into constants would hide the shape of the
      # manifest being generated.
      - linters:
          - goconst
        path: manifests/.*
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
